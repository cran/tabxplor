% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row-model.R
\name{new_lvl}
\alias{new_lvl}
\alias{is_lvl}
\title{A declared tabxplor label column}
\usage{
new_lvl(x, role = "level", var = NA_character_, ordered = NULL)

is_lvl(x)
}
\arguments{
\item{x}{A factor (or anything \code{factor()} accepts).}

\item{role}{One of \code{"level"}, \code{"var"}, \code{"tab_var"}.}

\item{var}{The variable name the labels belong to; \code{NA} on a merged \code{levels} column.}

\item{ordered}{A named logical vector, one entry per variable, saying whether that variable was
ordered in the source data. A single-variable column keeps its own \code{ordered} class as well.}
}
\value{
A \code{tabxplor_lvl} vector.
}
\description{
\code{tabxplor_lvl} is a light \strong{factor subclass} carrying what a row-index column is \emph{for}: its
\code{role} (\code{"level"} / \code{"var"} / \code{"tab_var"}), the \code{var} its labels belong to, and — per variable —
whether that variable was \code{ordered} in the source data. It is still a factor
(\code{is.factor()} is \code{TRUE}), so every base, dplyr and forcats operation keeps working unchanged.
}
\keyword{internal}
