% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_class.R
\name{fmt_attr}
\alias{fmt_attr}
\alias{fmt_attr<-}
\title{Read or write one \code{fmt} column attribute, by name}
\usage{
fmt_attr(x, name)

fmt_attr(x, name) <- value
}
\arguments{
\item{x}{A \code{tabxplor_fmt} vector, or a data.frame (then every \code{fmt} column is read).}

\item{name}{The attribute: one of \code{"scale"}, \code{"comp_all"}, \code{"ref"}, \code{"pct_type"}, \code{"col_var"},
\code{"col_group"}, \code{"totcol"}, \code{"refcol"}, \code{"color"}, \code{"color_signif"}, \code{"model_family"}, \code{"role"},
\code{"conf_level"}, \code{"degf"}, \code{"basis"}, \code{"ci_method"}. An unknown name is an error naming the set.}

\item{value}{The new value. Written through the attribute's own setter, so it is validated
exactly as \code{set_scale()} and friends validate it.}
}
\value{
The stored value (its declared default when the attribute is unset). On a data.frame, one
entry per \code{fmt} column, named. Writing returns the modified vector.
}
\description{
The generic form of the \verb{get_*()} / \verb{set_*()} family: one function covering every per-column
attribute a \code{tabxplor_fmt} vector carries, so a helper can loop over them instead of naming each.
The named accessors (\code{\link[=get_scale]{get_scale()}}, \code{\link[=get_col_var]{get_col_var()}}, \code{\link[=is_totcol]{is_totcol()}}, …) remain the readable way to
address one known attribute.
}
\examples{
x <- fmt(n = c(10, 20), pct = c(0.3, 0.7), scale = "level_pct", pct_type = "row")
fmt_attr(x, "scale")
fmt_attr(x, "col_var") <- "region"
fmt_attr(x, "col_var")
}
\seealso{
\code{\link[=tab_columns]{tab_columns()}} for every column's attributes at once; \code{\link[=fmt]{fmt()}} for what each one means.
}
