% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_class.R
\name{fmt_fields}
\alias{fmt_fields}
\alias{get_num}
\alias{set_num}
\alias{is_totrow}
\alias{get_row_kind}
\alias{set_row_kind}
\alias{as_totrow}
\alias{is_tottab}
\alias{as_tottab}
\alias{set_display}
\alias{is_refrow}
\alias{as_refrow}
\alias{get_digits}
\alias{get_pvalue}
\alias{set_digits}
\alias{set_pvalue}
\title{Per-cell fields of a \code{fmt} vector}
\usage{
get_num(x)

set_num(x, value)

is_totrow(x, ...)

get_row_kind(x)

set_row_kind(x, row_kind)

as_totrow(x, in_totrow = TRUE)

is_tottab(x, ...)

as_tottab(x, in_tottab = TRUE)

set_display(x, value)

is_refrow(x, ...)

as_refrow(x, in_refrow = TRUE)

get_digits(x)

get_pvalue(x)

set_digits(x, value)

set_pvalue(x, value)
}
\arguments{
\item{x}{The object to test, to get a field in, or to modify.}

\item{value}{The value you want to inject in some \code{fmt} vector's vctrs::field
or attribute using a given "set" function.}

\item{...}{In \code{fmt()}, it exists only for the arguments retired in tabxplor 2.0.0:
\code{type} is translated into \code{scale} + \code{pct_type} (see
\code{\link{tabxplor-type}}), \code{ci_type} gets an error naming its replacement. In the
accessor methods below, to add arguments in the future.}

\item{row_kind}{The kind of row a cell sits in (see \code{\link{get_row_kind}}).}

\item{in_totrow}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{row_kind = "total"}.}

\item{in_tottab}{\code{TRUE} when the cell is part of a total table}

\item{in_refrow}{\code{TRUE} when the cell is part of a reference row
(cf. \code{ref})}
}
\value{
A getter returns a vector the length of \code{x}; a setter the modified \code{fmt} vector. Given a
data.frame, a getter answers once per \code{fmt} column.
}
\description{
Read and write the values that vary from cell to cell: the displayed number, its decimals, its
p-value, and the kind of row each cell sits in. Every \code{fmt} vector carries all of them, an
inapplicable one stored as \code{NA} --- so these always answer, even on a column where the field
means nothing.

Use them on a single \code{fmt} vector or, through \code{dplyr::across(where(is_fmt), ...)}, on a whole
table. To reach a field these do not name, use \verb{x$<field>} or \code{\link[vctrs:field]{vctrs::field()}}.
}
\section{Functions}{
\itemize{
\item \code{get_num()}: get the currently displayed field

\item \code{set_num()}: set the currently displayed field (not changing display type)

\item \code{is_totrow()}: test function to detect cells in total rows
(at \code{fmt} level or \code{tab} level)

\item \code{get_row_kind()}: get the "row_kind" field: what kind of row each cell sits in
(one of \code{"data"}, \code{"total"}, \code{"n"}, \code{"pct"}, \code{"pvalue"},
\code{"gof"}, \code{"blank"}).

\item \code{set_row_kind()}: set the "row_kind" field

\item \code{as_totrow()}: set the "total" row kind (belong to total row)

\item \code{is_tottab()}: test function to detect cells in total tables
(at \code{fmt} level or \code{tab} level)

\item \code{as_tottab()}: set the "in_tottab" field (belong to total table)

\item \code{set_display()}: set the "display" vctrs::field of a \code{fmt} vector, or of
all of them in the whole tibble.

\item \code{is_refrow()}: test function to detect cells in reference rows
(at \code{fmt} level or \code{tab} level)

\item \code{as_refrow()}: set the "in_refrow" field (belong to reference row)

\item \code{get_digits()}: get the "digits" field

\item \code{get_pvalue()}: get the per-cell p-value (what the significance stars read)

\item \code{set_digits()}: set the "digits" field

\item \code{set_pvalue()}: set the per-cell p-value. \code{set_pvalue(x, NA_real_)} is how a duplicated,
purely descriptive copy of a column loses its stars: the stored p-value is their only source.

}}
\seealso{
\code{\link[=fmt]{fmt()}} for what every field means and how to build a cell; \link{fmt_attributes} for the
per-column facts; \code{vignette("tabxplor-programming")}.
}
