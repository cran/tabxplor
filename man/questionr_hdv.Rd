% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{questionr_hdv}
\alias{questionr_hdv}
\title{Histoire de vie: leisure, work and beliefs in France, 2003}
\format{
A tibble of 2 000 rows and 20 columns. The ones the article uses:
\describe{
\item{cinema}{Went to the cinema in the last 12 months? "Oui" first.}
\item{qualif}{Occupational qualification, 7 levels, "Cadre" (senior professional) first.
347 values are missing, as in the original.}
\item{age}{Age in years, 18 to 97.}
\item{poids}{The survey's own sampling weight.}
}
The rest describe the respondent (\code{sexe}, \code{nivetud}, \code{occup}, \code{freres.soeurs}), their views
(\code{clso}, \code{relig}, \code{trav.imp}, \code{trav.satisf}) and six more leisure activities (\code{sport}, \code{cuisine},
\code{bricol}, \code{lecture.bd}, \code{peche.chasse}, \code{hard.rock}), plus \code{heures.tv} and \code{id}.
}
\source{
The complete \code{hdv2003} data of the \pkg{questionr} package (Julien Barnier, Francois
Briatte and Joseph Larmarange), GPL (>= 2) -- with thanks. It comes from the \emph{Histoire de vie}
survey run in 2003 by INSEE, the French national statistics institute. tabxplor's copy changes
only the level order: every "Oui"/"Non" item reads "Oui" first, and \code{qualif} starts at "Cadre".
}
\usage{
questionr_hdv
}
\description{
A French national survey, used to show what \strong{adjustment} does to a relationship: going to the
cinema is strongly patterned by occupation, and also by age -- and the two are entangled. See the
\emph{Reading a regression} article for the analysis. It also carries a real sampling weight (\code{poids}), so it
is the data set to try \verb{wt =} on; see \code{vignette("tabxplor-weights")}.
}
\examples{
tab(questionr_hdv, qualif, cinema, pct = "row", na = "drop", color = "difference")
}
\keyword{datasets}
