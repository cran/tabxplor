% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-structure.R
\name{tab_structure}
\alias{tab_structure}
\title{The structure of a table}
\usage{
tab_structure(x)
}
\arguments{
\item{x}{A \code{tabxplor_tab} / \code{tabxplor_grouped_tab}, or a list of them (\code{output_list = TRUE}).}
}
\value{
A named list:
\describe{
\item{\code{container}}{\code{"table"} or \code{"list"}.}
\item{\code{kind}}{\code{"crosstab"} or \code{"regression"} (\code{NA} when the table carries no metadata).}
\item{\code{merged}}{\code{TRUE} when several row variables are stacked in one table (a \code{var} column
names each row's variable).}
\item{\code{grouped}}{\code{TRUE} when the table has \code{tab_vars} (sub-tables).}
\item{\code{row_vars}, \code{tab_vars}, \code{col_vars}}{the variables on each axis.}
\item{\code{same_col_vars}, \code{same_tab_vars}}{for a list only: whether its tables agree.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

What kind of object a \code{tabxplor} result is, read from its own declared model — the row-index
columns (their stored roles) and the table's stated kind — rather than guessed from column names.
}
\examples{
\donttest{
t <- tab(forcats::gss_cat, c(marital, relig), race, pct = "row")
tab_structure(t)$merged
}
}
\seealso{
\code{\link[=tab_columns]{tab_columns()}} for the column-axis view.
}
