% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-footer.R
\name{tab_footer_text}
\alias{tab_footer_text}
\title{Read the footer a table would print}
\usage{
tab_footer_text(
  x,
  medium = c("plain", "console", "html", "md"),
  style = NULL,
  lang = NULL,
  theme = NULL
)
}
\arguments{
\item{x}{A \code{tabxplor_tab}.}

\item{medium}{One of \code{"plain"} (default), \code{"console"}, \code{"html"}, \code{"md"}.}

\item{style}{\code{"terse"} (the console's compact one-liner) or \code{"prose"} (full sentences,
the exports' default); \code{NULL} follows the medium.}

\item{lang}{\code{NULL} (from \code{getOption("tabxplor.lang")}), \code{"en"} or \code{"fr"}.}

\item{theme}{Palette theme; \code{NULL} follows the medium's option.}
}
\value{
A character vector, one element per footer line.
}
\description{
The lines \pkg{tabxplor} prints under a table, resolved: the template's placeholders built, the
user's own lines as written. It is what \code{\link[=set_subtext]{set_subtext()}} edits, after the fact.
}
\examples{
t <- tab(forcats::gss_cat, race, marital, pct = "row", color = "diff")
cat(tab_footer_text(t), sep = "\n")
}
\seealso{
\code{\link[=set_subtext]{set_subtext()}} for the template, \code{\link[=set_legend_words]{set_legend_words()}} for the words.
}
