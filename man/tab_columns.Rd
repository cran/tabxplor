% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-structure.R
\name{tab_columns}
\alias{tab_columns}
\title{Every \code{fmt} column of a table, and what it carries}
\usage{
tab_columns(x)
}
\arguments{
\item{x}{A \code{tabxplor_tab} / \code{tabxplor_grouped_tab}, or any data.frame holding \code{fmt} columns.}
}
\value{
A tibble, one row per \code{fmt} column:
\describe{
\item{\code{column}}{the column name.}
\item{\code{col_var}, \code{col_group}}{the column variable, and the sub-population its block belongs to
(\code{""} when the table was never spread).}
\item{\code{scale}, \code{pct_type}}{what the column estimates, and on which percentage base.}
\item{\code{display}}{the display template(s) its cells carry.}
\item{\code{ref}, \code{comp_all}, \code{totcol}, \code{refcol}}{the comparison model: which baseline, whether it
compares across sub-tables, and whether this column is a total or the reference.}
\item{\code{color}, \code{color_bg}, \code{color_signif}}{the colour measure of each channel and the
significance policy.}
\item{\code{conf_level}, \code{degf}, \code{basis}, \code{ci_method}}{how this column's interval was computed — the
level, the degrees of freedom it is referred to (\code{NA} = the normal quantile), whether it rests
on the raw count, the weights or the survey design, and by which method.}
\item{\code{model_family}, \code{role}}{for a \code{\link[=tab_reg]{tab_reg()}} table: the column's model family, and whether it
holds the model estimate (\code{"model"}) or its observed counterpart (\code{"emp"}).}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

One row per numeric (\code{tabxplor_fmt}) column, with the per-column attributes that decide what it
shows, what it estimates, how it is coloured and how its confidence interval was computed. The
column-axis companion of \code{\link[=tab_structure]{tab_structure()}}, which describes the table as a whole.
}
\examples{
\donttest{
t <- tab(forcats::gss_cat, marital, race, pct = "row", ci = "ref")
tab_columns(t)
}
}
\seealso{
\code{\link[=tab_structure]{tab_structure()}} for the table's own structure; \code{\link[=fmt_attr]{fmt_attr()}} to read or write one attribute;
\code{\link[=fmt]{fmt()}} for what each attribute means.
}
