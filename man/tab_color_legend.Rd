% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-legend.R
\name{tab_color_legend}
\alias{tab_color_legend}
\title{Build the colour legend of a table}
\usage{
tab_color_legend(
  x,
  medium = c("console", "html", "md", "runs", "plain"),
  style = NULL,
  lang = NULL,
  colored = TRUE,
  theme = NULL,
  classes = FALSE
)
}
\arguments{
\item{x}{A \code{tabxplor_tab}.}

\item{medium}{One of "console", "html", "md", "runs", "plain". \code{"runs"} is for the media that
draw the legend as coloured TEXT and cannot fill, such as an Excel rich-text cell
(\code{\link{tab_xl}}). It returns the runs unrendered, and draws the
background channel from the darker \code{bg_legend} palette (see \code{\link{set_color_palette}}).}

\item{style}{"terse" (compact, console default) or "prose" (full sentences, export default).}

\item{lang}{NULL (auto from locale) / "en" / "fr".}

\item{colored}{Whether to colour the break-words.}

\item{theme}{Palette theme (default from options).}

\item{classes}{\code{medium = "html"} only: emit the break-words as CSS slot classes rather than inline
hex, because a tabxplor stylesheet ships with the output (\code{tab_html()}). Then the
legend follows a theme toggle exactly like the cells it describes. \code{FALSE} (a table rendered
without a stylesheet of ours) keeps inline hex.}
}
\value{
A character vector (or, for "runs", a list of run-lists), or NULL when nothing is coloured.
}
\description{
Internal. Returns one legend line per colour-signature group. For \code{medium = "runs"} each line
is a list of runs \code{list(text, color, bold)}; otherwise a character string.
}
\keyword{internal}
