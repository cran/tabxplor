% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-options.R
\name{tabxplor-options}
\alias{tabxplor-options}
\alias{tabxplor.options}
\title{The tabxplor options, and their defaults}
\description{
\code{tabxplor} reads its display, colour, statistics and export defaults from \code{options()}, all
prefixed \code{tabxplor.}. Set any of them for a session with \code{\link[=options]{options()}}, e.g.
\code{options(tabxplor.stars = TRUE)}, or once at the top of a script or \code{.Rmd}. The defaults are
established when the package loads (\code{.onLoad()}); most also have a per-call argument on the
relevant function, which always wins over the option.
}
\section{Display and printing}{

\describe{
\item{\code{tabxplor.print}}{\code{"console"} (default): how a table auto-prints. \code{"html"} renders the \code{\link[=tab_html]{tab_html()}} table (in the Viewer pane in RStudio/Positron, and as a real html table in rmarkdown/Quarto documents) --- recommended when you work in an IDE with a Viewer. \code{"kable"} is an accepted synonym of \code{"html"} (the pre-2.0.0 name).}
\item{\code{tabxplor.stars}}{\code{FALSE} (default): whether cells show significance stars, and at which cut-offs. \code{FALSE} (no stars), \code{TRUE} (the default ladder \code{c("*" = 0.10, "**" = 0.05, "***" = 0.01)}), or a named numeric giving your own -- names are the glyphs, values the p-value cut-offs, e.g. \code{options(tabxplor.stars = c("*" = 0.05, "**" = 0.01))}. Off for \code{\link[=tab]{tab()}}, on for \code{\link[=tab_reg]{tab_reg()}}. The LADDER is a render-time reading of each cell's stored p-value, so it is this option alone --- change it and every table already built shows the new glyphs. Per-call \verb{stars =}.}
\item{\code{tabxplor.ratio_print}}{\code{"inverse"} (default): prints a multiplicative value below its reference as the inverse --- an odds ratio of 0.37 as \code{1/2.67}, a mean ratio of 0.42 as \verb{/2.4} --- so "2.7 times less" reads as strongly as "2.7 times more", and the same in a bracket. \code{"raw"} prints the plain number (\code{0.37}), the convention of most journals.}
\item{\code{tabxplor.n}}{\code{"range"} (default): how many people a table says are behind its numbers. \code{"range"} puts the unweighted base beside the Total cell of a crosstab (\verb{100\% (9 838)}) and in the \code{n} column of a regression table, printed as \code{min-max} when the blocks rest on different populations --- several column variables losing different \code{NA}s, or several models. \code{"min"} prints the smallest base only, \code{"no"} shows no count at all. It replaces the \code{add_n} argument, deprecated in 2.0.0. Per-call \verb{n =}.}
\item{\code{tabxplor.color_whole_cell}}{\code{FALSE} (default): EXPERT. A cell that prints SEVERAL fields reads as one number with an aside --- \verb{1/1.63*** (31\%)} --- so the cell's rendering grades the number and the aside is set slightly back from the table's own text, following the theme. That covers the colour and, under \code{theme = "print"}, the typography (bold, italic, underline) alike. Set to \code{TRUE} to extend the primary's own rendering over the whole cell instead (the pre-2.0.0 look). There is nothing to choose beyond that: which grey an aside takes belongs to the theme's palette, not to a per-cell option --- see \code{set_color_palette()}. Console, html and Markdown; Excel renders a cell as a whole either way.}
\item{\code{tabxplor.var_names}}{\code{"both"} (default): which variable names the exporters annotate: \code{"both"}, \code{"rows"}, \code{"cols"}, \code{"none"}. Per-call \verb{var_names =}.}
\item{\code{tabxplor.var_labels}}{\code{FALSE} (default): in \emph{exports} (markdown / html / Excel / plot), show a variable's \emph{label} (the \code{haven}/\code{labelled} \code{label} attribute, if it has one) instead of its name. Display only -- the table structure keeps canonical names, so name-based \code{select()} and references still work; the console always shows names.}
\item{\code{tabxplor.cleannames}}{\code{FALSE} (default): clean up variable/level names in output. Also strips a \code{"1-"}-style prefix from \code{labelled} value labels turned into factor levels. Per-call \verb{cleannames =}.}
\item{\code{tabxplor.total_names}}{\code{c(row = "Total", col = "Total", tab = "Ensemble", other = "Others")} (default): the four synthetic labels a table carries: \code{row} and \code{col} name the total row and the total column, \code{tab} the total \emph{table} (the one made when there are \code{tab_vars}), and \code{other} the level \code{other_if_less_than} lumps small levels into. A partial vector is allowed -- \code{options(tabxplor.total_names = c(tab = "Ensemble", other = "Autres"))} leaves the first two alone. It replaces the \code{total_names} / \code{totaltab_name} / \code{other_level} arguments, deprecated in 2.0.0.}
\item{\code{tabxplor.shape_auto_max}}{\code{12L} (default): where \code{shape = "auto"} draws the line for a numeric row or tab variable: a column with at most this many distinct \strong{whole} values is a counted number or a short scale, and keeps one level per value; anything else is continuous and is cut into \code{"sd_bands"}. Raise it for a long scale, lower it to band more eagerly --- or name the variable in \code{shape} and decide yourself.}
}
}

\section{Colours and theme}{

\describe{
\item{\code{tabxplor.color_breaks}}{the colour-break scales (a named list of \code{pct_diff}, \code{pct_ratio}, \code{odds_ratio}, \code{mean_diff}, \code{mean_ratio}, \code{contrib}, \code{zscore}, \code{adj_ratio}, \code{adj_diff}, \code{adj_diff_std}). Set with \code{\link[=set_color_breaks]{set_color_breaks()}}. Per-call \verb{color_breaks =}.}
\item{\code{tabxplor.color_style_theme} (alias \code{tabxplor.console_theme})}{the \emph{console} palette theme, \code{"light"} or \code{"dark"}; set by \code{\link[=set_color_palette]{set_color_palette()}} (which auto-detects the editor theme on load). NOT the export theme (\code{tabxplor.theme} / \code{tabxplor.export_theme}).}
\item{\code{tabxplor.console_bold}}{whether to embolden the reference / total (and coloured) cells in the \emph{console}, \code{TRUE} or \code{FALSE}. Auto-detected at load: \code{TRUE} in Positron and VS Code (which render ANSI bold at a fixed glyph width), \code{FALSE} in RStudio and unknown consoles (there bold is drawn wider and would break column alignment). Override it for your own front-end / font.}
\item{\code{tabxplor.theme} (alias \code{tabxplor.export_theme})}{\code{"light"} (default): the \emph{export} theme: \code{"light"}, \code{"dark"}, \code{"auto"} (follow the reader), or a black-and-white \strong{publication} palette -- \code{"print_ready"} picks one per table (marks for a cross-table, the emphasis ladder for a regression), or name it yourself: \code{"print_marks"}, \code{"print_emphasis"}, \code{"print_minimalistic"} (\code{"bw"} is a synonym of the last). See \code{\link[=tab_css]{tab_css()}} for what each says. \code{"auto"} needs a stylesheet, so only \code{\link[=tab_html]{tab_html()}}, \code{\link[=tab_md]{tab_md()}} and \code{\link[=tab_css]{tab_css()}} honour it; static backends resolve it to \code{"light"}. A publication palette reaches every backend, Excel included. Per-call \verb{theme =}.}
\item{\code{tabxplor.print_rules}}{\code{TRUE} (default): every stylesheet \code{\link[=tab_css]{tab_css()}} emits also carries a black-and-white publication palette inside an \verb{@media print} block, so a table rendered in colour \strong{prints} (or saves to PDF) publication-ready with no further action. Set \code{FALSE} if your printer is a colour one and the colours are the point, or name a palette (\code{"print_emphasis"}) to print in that one instead of the default \code{"print_minimalistic"}. \code{"print_marks"} and \code{"print_ready"} cannot be used here: their marks are cell text, and a print rule can restyle a page but not add characters to it. Per-call \verb{print_rules =}.}
\item{\code{tabxplor.background}}{\code{"page"} (default): what a rendered table paints behind itself. \code{"page"} leaves it \strong{transparent}, so the table sits on the page's own ground, whatever that is; \code{"theme"} paints the theme's own background, a card of its own; or name any CSS colour. Change it only where the page is not yours to follow --- a dark table dropped into a light document, an html email. The interactive Viewer page paints itself either way, and a \strong{publication} palette is always a sheet of white paper.}
}
}

\section{Statistics and confidence intervals}{

\describe{
\item{\code{tabxplor.anova}}{\code{"welch"} (default): which one-way ANOVA F is shown for mean columns: \code{"welch"} (robust) or \code{"classic"} (pooled variance). Both are always stored in the \code{test} attribute. Per-call \verb{anova =}.}
\item{\code{tabxplor.design_effect}}{\code{FALSE} (default): a weighted \code{\link[=tab]{tab()}} estimates the population but bases every interval and test on the raw number of respondents, so they carry no design effect --- and the table's footer says so. Set \code{TRUE} and the same intervals \strong{account for the unequal weighting, exactly}: a weight column IS a survey design (the flat one, \code{ids = ~1}), whose variance has a closed form in the per-cell \code{sum(w^2)} the aggregate already computes, so the base becomes \code{n_eff = p(1-p) / Var_design(p)} in \strong{every weighted descriptive confidence interval} --- factor proportions \emph{and} means (cell, difference, ratio and the \code{color = "odds_ratio"} significance) --- and the whole-table tests (\code{test = TRUE}) become \code{survey::svychisq} / a \code{svyglm} Wald F on that flat design. It reproduces \code{survey} to the last digit, Kish's \verb{(sum w)^2 / sum(w^2)} being that same formula with each cell's own \code{sum(w^2)} discarded. Being exact rather than a bound, it can make an interval \emph{narrower} as well as wider. It is blind to \strong{clustering} and to \strong{calibration}, which the weights do not record --- and those are not symmetric: missing the calibration and the strata costs a few percent, in the safe direction, while missing the clusters of a face-to-face household survey can leave an interval several times too short (see the Weights section of \code{vignette("tabxplor")}). It needs the microdata weights, so \code{\link[=tab_counts]{tab_counts()}} on pre-aggregated counts cannot apply it (such a table states the raw basis in its footer rather than claiming a correction it does not have). \strong{Scope: \code{\link[=tab]{tab()}} and its leaves only.} \code{\link[=tab_reg]{tab_reg()}} never reads it --- its crude \verb{empirical =} companions are always on the weighted basis, beside a model column (\code{survey::svyglm}) that always was. For the full design effect --- strata, clusters, \code{fpc}, calibration --- pass a \code{survey::svydesign} as \code{data}; the option is then not consulted at all. Per-call \verb{design_effect =}.}
\item{\code{tabxplor.conf_level}}{\code{0.95} (default): confidence level for the intervals and significance tests. Since 2.0.0 each column records the level it was built at, so the colour thresholds follow the argument and this option is the fallback for a column that never recorded one (a hand-built \code{\link[=fmt]{fmt()}}, or a table from an older session). Per-call \verb{conf_level =}.}
\item{\code{tabxplor.legend_style}}{\code{"prose"} (default): the colour-legend style in exports (\code{\link[=tab_md]{tab_md()}}, \code{\link[=tab_html]{tab_html()}}, \code{\link[=tab_xl]{tab_xl()}}): \code{"prose"} (full sentences) or \code{"terse"} (the compact one-line form the console uses). The console itself is always terse.}
\item{\code{tabxplor.test_lines}}{\code{"summary"} (default): how many crosstab test rows the exporters (\code{\link[=tab_md]{tab_md()}}, \code{\link[=tab_html]{tab_html()}}, \code{\link[=tab_xl]{tab_xl()}}) append: \code{"summary"} (p-value + effect size), \code{"all"} (+ the raw statistic), \code{"stat"} (p-value + statistic), or \code{"pvalue"} (the single p-value row). The p-value row name states the test used ("pvalue (Chi2, Welch F; survey-design)") and the effect-size row its measure ("Cramer's V, eta2"). N is never added -- it is already shown by the \code{n} column. The console block always shows N + p-value + effect size.}
\item{\code{tabxplor.shape_table} (alias \code{tabxplor.spark})}{\code{"all"} (default): in a \code{\link[=tab_reg]{tab_reg()}} table, each continuous predictor's OBSERVED SHAPE --- the outcome binned against the predictor, on the model's scale, with no model in it --- drawn as a small curve in a \strong{shape table} below the footer, beside the range it is a picture of (\verb{13-57\% (OR 8.7)}). It is the eye-half of the \code{Linearity} footer row, and the free one: no fit is involved. With \code{tab_vars}, one curve per group; with several outcomes, one per outcome.  \code{"all"} draws it in every medium; \code{"console"} only where you are working, so exported tables stay unchanged; \code{"no"} never. \code{TRUE} / \code{FALSE} are accepted for the first and the last.  The curve is drawn TO SCALE on the predictor as the model sees it, so a \code{shape} transform visibly straightens it when it is the right cure, and every predictor's curve is the same width. Its vertical window is floored by the data's own sampling noise, so a curve smaller than that is greyed and marked \code{ns} --- read it as a flat line whatever its shape. In HTML the glyphs become an inline SVG; a plot never draws them (no graphics-device font has them).  An ordinal or multinomial outcome has one curve per cut or per category and this draws only the first: \code{\link[=reg_check_plots]{reg_check_plots()}} shows them all.}
}
}

\section{HTML / \code{tab_html()} export}{

\describe{
\item{\code{tabxplor.tab_kable_css} (alias \code{tabxplor.kable_css})}{\code{TRUE} (default): inline the stylesheet with each \code{\link[=tab_html]{tab_html()}} / \code{\link[=tab_md]{tab_md()}} table (self-contained). Set \code{FALSE} in a many-table document that emits \code{\link[=tab_css]{tab_css()}} once at the top. Per-call \verb{css =}.}
\item{\code{tabxplor.tab_kable_tooltips}}{\code{TRUE} (default): show the per-cell hover tooltips (counts, confidence intervals, differences...) in html tables. Set \code{FALSE} once per document when every table auto-prints and tooltips are unwanted. Per-call \verb{tooltips =}.}
\item{\code{tabxplor.kable_popover}}{\code{FALSE} (default): use click popovers instead of hover tooltips. Per-call \verb{popover =}.}
\item{\code{tabxplor.tab_kable_num_font}}{the HTML/markdown number-font CSS stack. Monospace by default so figures stay column-aligned (set a proportional stack to revert).}
\item{\code{tabxplor.output_kable}}{\code{FALSE} (default): make \code{\link[=tab]{tab()}} render its result with \code{\link[=tab_html]{tab_html()}} before returning it --- a convenience for \code{.Rmd}/\code{.qmd} documents. Since 2.0.0 it only \emph{renders}: it no longer changes the shape of the built object (that is \code{output_list}).}
}
}

\section{Excel / \code{tab_xl()} export}{

\describe{
\item{\code{tabxplor.xl_font_text}}{\code{"DejaVu Sans Condensed"} (default): text (labels/headers) font. Per-call \verb{font_text =}.}
\item{\code{tabxplor.xl_font_num}}{\code{"DejaVu Sans"} (default): number font without stars. xlsx records ONE name (no fallback list), so set a font installed where the workbook is opened. Per-call \verb{font_num =}.}
\item{\code{tabxplor.xl_font_num_stars}}{\code{"Cascadia Mono"} (default): number font with stars (monospace, so stars align). Per-call \verb{font_num_stars =}.}
\item{\code{tabxplor.xl_ratio_cells}}{\code{"fold"} (default): what a ratio / odds-ratio cell HOLDS in the workbook: \code{"fold"} (the default) the signed fold, so Excel prints what the console prints and the cell stays a number; \code{"raw"} the untransformed ratio; \code{"text"} the exact display string. Per-call \verb{ratio_cells =}.}
}
}

\section{Plot, paths and language}{

\describe{
\item{\code{tabxplor.export_dir}}{\code{NULL} (default): default directory for exported files (\code{NULL} = the working / typed path).}
\item{\code{tabxplor.lang}}{\code{"auto"} (default): the colour-legend language: \code{"auto"} (follows the R/OS locale), \code{"en"} or \code{"fr"}. Per-call \verb{lang =}.}
}
}

\section{Parallel build}{

\describe{
\item{\code{tabxplor.parallel}}{\code{FALSE} (default): build the independent units of one call on parallel CPU cores (needs the \code{mirai} package): the per-\code{row_var} tables of a \code{\link[=tab]{tab()}}, the models / \code{tab_vars} groups / outcomes of a \code{\link[=tab_reg]{tab_reg()}}. The result is byte-identical to the serial one.  \code{FALSE} (default) never dispatches. \code{"auto"} (or \code{TRUE}) takes \strong{half the cores this session may actually use, at least 2 and at most 4} --- so 2 on a dual-core laptop, 2 on a 4-core machine, 4 on 8 cores or more. An integer takes that many verbatim. The count respects \code{options(mc.cores)}, a container's CPU quota and an HPC allocation (it reads them through the \code{parallelly} package when installed), and never exceeds 2 under \verb{R CMD check}.  It stays OPT-IN because starting the pool BLOCKS for about a second, so the first parallel table of a session is always slower than the serial one; from the third it is ahead. It pays off for MANY evenly sized units against a small or medium data frame --- 24 tables run about 2.8x faster on 4 workers --- and is a loss for few units or multi-million-row data, where shipping the population to each worker eats the gain. Set it once at the top of a script: \code{options(tabxplor.parallel = "auto")}. A model comparison (\code{stats = "compare_*"}) is always serial and says so when asked: it is a test BETWEEN the fits, so they are built together. For one call only, wrap it in \code{withr::with_options(list(tabxplor.parallel = "auto"), ...)}. The pool persists for the session; release it with \code{\link[=tab_parallel_stop]{tab_parallel_stop()}}.}
\item{\code{tabxplor.parallel_min}}{\code{2L} (default): the smallest UNIT count worth dispatching -- \code{row_var}s for \code{\link[=tab]{tab()}}, models for \code{\link[=tab_reg]{tab_reg()}} (fewer runs serially, since the setup would outweigh the gain).}
}
}

