% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_classes.R
\name{tabxplor-base-coercion}
\alias{tabxplor-base-coercion}
\alias{as.matrix.tabxplor_tab}
\alias{as.table.tabxplor_tab}
\title{Hand a table to base R}
\usage{
\method{as.matrix}{tabxplor_tab}(x, totals = FALSE, ...)

\method{as.table}{tabxplor_tab}(x, totals = FALSE, ...)
}
\arguments{
\item{x}{A table made with \code{\link{tab}}, \code{\link{tab_counts}} or \code{\link{tab_reg}}.}

\item{totals}{Set to \code{TRUE} to keep the total row, the total columns and the display-time rows.}

\item{...}{Not used.}
}
\value{
A numeric \code{matrix}, or a base \code{table}.
}
\description{
\code{as.matrix()} gives the table's numbers as a plain numeric matrix; \code{as.table()} gives the same
matrix as a base \code{\link[base]{table}}, its \code{dimnames} named after the row and column
variables. That is the shape base R and the packages built on it expect --- a correspondence
analysis, \code{chisq.test()}, \code{mosaicplot()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{FactoMineR::CA(as.matrix(tab(forcats::gss_cat, race, marital)), graph = FALSE)
}\if{html}{\out{</div>}}

Only the DATA cells come across. The total row, the total columns and the display-time rows (the
base count, \code{add_pct}, the p-value and model-fit lines) are dropped, because a test or an analysis
run on a table's own margins is wrong; \code{totals = TRUE} keeps them. Each cell contributes the
number it \emph{shows}, so a plain \code{\link{tab}} gives counts, a \code{pct = "row"} table proportions,
and a numeric column means.
}
\section{Functions}{
\itemize{
\item \code{as.matrix(tabxplor_tab)}: the table's numbers as a numeric matrix

\item \code{as.table(tabxplor_tab)}: the same, as a base \code{table} with named dimnames

}}
\examples{
tabs <- tab(forcats::gss_cat, race, marital)
as.matrix(tabs)
as.table(tabs)

# a row-percentage table gives proportions, not counts:
as.matrix(tab(forcats::gss_cat, race, marital, pct = "row"))
}
\seealso{
\code{\link{get_num}}, \code{\link{tab_export}}.
}
