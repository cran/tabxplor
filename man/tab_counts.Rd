% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-counts.R
\name{tab_counts}
\alias{tab_counts}
\title{Cross-tables from already-aggregated counts}
\usage{
tab_counts(
  data,
  row_var,
  col_var,
  tab_vars,
  counts,
  wt_counts,
  cols,
  col_name = "variable",
  base,
  input = c("counts", "pct"),
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{row_var, col_var}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Singular aliases of
\code{row_vars}/\code{col_vars} (which now accept several variables). Kept working.}

\item{tab_vars}{<\link[tidyr:tidyr_tidy_select]{tidy-select}> Tab variables: one subtable per
combination of their levels. Leave empty for a simple cross-table.}

\item{counts}{The column holding the \strong{unweighted} count for each cell (long tidy shape).}

\item{wt_counts}{Optional column holding the \strong{weighted} count for each cell. Leave empty for an
unweighted table.}

\item{cols}{<\code{\link[tidyr:tidyr_tidy_select]{tidy-select}}> For a wide \code{data.frame}: the columns
holding the \code{col_var} levels.}

\item{col_name}{Name of the (synthesised) column variable when \code{cols} is used.}

\item{base}{For \code{input = "pct"}: the column holding each row's sample size N.}

\item{input}{\code{"counts"} (default) or \code{"pct"} (with \code{cols} and \code{base}: the level columns hold
frequencies, and counts are rebuilt from them and \code{base}).}

\item{...}{Every other argument of \code{\link[=tab]{tab()}} -- \code{pct}, \code{color}, \code{ci}, \code{tot}, ... -- passed
by name. See \code{\link[=tab]{tab()}}; a typo gets a suggestion.}
}
\value{
A \code{tabxplor_tab} (or \code{tabxplor_grouped_tab} when \code{tab_vars} are provided).
}
\description{
\code{tab_counts()} builds the same color-coded cross-table as \code{\link[=tab]{tab()}}, but from data that is
\strong{already cross-tabulated} --- a \code{dplyr::count()} result, a contingency table, or a published
table of counts or percentages --- instead of microdata (one row per individual). Percentages,
differences, confidence intervals, chi-squared, colors and totals are all computed from the
counts, and the result is identical to the table \code{\link[=tab]{tab()}} would build from the microdata behind
them.

It accepts four input shapes:
\itemize{
\item \strong{Long tidy counts} (the default): one row per \code{row_var} \eqn{\times} \code{col_var} (\eqn{\times}
\code{tab_vars}) combination, with the count in \code{counts} (and the weighted count in \code{wt_counts}).
\item \strong{A wide \code{data.frame}}: a label (\code{row_var}) column plus one column per \code{col_var} level ---
select those level columns with \code{cols} and name the column variable with \code{col_name}.
\item \strong{A \code{table} / \code{xtabs} / \code{matrix} object}: melted automatically, the row and column variables
read from the dimnames (or set with \code{row_var} / \code{col_var}).
\item \strong{Frequencies + base N}: the wide shape, plus \code{input = "pct"} and \code{base} (the column of row
sample sizes); the integer counts are rebuilt from the percentages and the base.
}

With weighted data, give the real (unweighted) count in \code{counts} \strong{and} the weighted count in
\code{wt_counts}: estimates use the weighted counts, while confidence intervals and tests use the
real unweighted sample size. Counts that are not whole numbers (weighted-only or frequency-only
input) disable confidence intervals and chi-squared, with a message.
}
\examples{
# Long tidy counts (as from dplyr::count()) reproduce the microdata table :
counts <- dplyr::count(forcats::gss_cat, marital, race)
tab_counts(counts, marital, race, counts = n, pct = "row")
# tab(forcats::gss_cat, marital, race, pct = "row")   # identical

# A contingency table object :
tab_counts(table(forcats::gss_cat$marital, forcats::gss_cat$race), pct = "row")

# A wide data.frame of counts :
wide <- tidyr::pivot_wider(counts, names_from = race, values_from = n)
tab_counts(wide, row_var = marital, cols = c(Other, Black, White),
           col_name = "race", pct = "row")
}
