% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-deprecate.R
\name{tabxplor-type}
\alias{tabxplor-type}
\alias{set_type}
\alias{get_type}
\title{Column types, the tabxplor 1.x spelling}
\usage{
set_type(x, type)

get_type(x, ...)
}
\arguments{
\item{x}{A \code{fmt} vector, or a data frame of them.}

\item{type}{One of \code{"row"}, \code{"col"}, \code{"all"}, \code{"all_tabs"}, \code{"mean"}, \code{"n"}, \code{"coef"}.}

\item{...}{Used in methods to add arguments in the future.}
}
\value{
\code{get_type()} a character vector; \code{set_type()} a modified \code{fmt} vector.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}}

In tabxplor 1.x a \code{fmt} column carried one \code{type} attribute, whose seven values conflated two
facts. Since 2.0.0 they are two attributes: \code{\link[=get_scale]{get_scale()}} / \code{\link[=set_scale]{set_scale()}} say \strong{what the column
estimates} (a key into the declared scale table), and \code{\link[=get_pct_type]{get_pct_type()}} / \code{\link[=set_pct_type]{set_pct_type()}} say
\strong{which kind of percentage} it holds. These two functions translate, so 1.x code keeps running;
they are defunct in tabxplor 2.1.0.

\code{get_type()} re-fuses what 2.0.0 split, so it is a reading aid rather than an accessor:
\code{level_mean} reads back \code{"mean"}, \code{level_n} reads \code{"n"}, a level percentage reads its own
\code{pct_type}, and every effect scale (a difference, a ratio, an odds ratio, a coefficient) reads
\code{"coef"} --- distinctions 1.x could not make are lost on the way back.
}
\section{Functions}{
\itemize{
\item \code{set_type()}: set the retired \code{type} attribute of a \code{fmt} vector

\item \code{get_type()}: get the retired \code{type} of \code{fmt} columns

}}
\examples{
x <- fmt(n = c(10, 20), pct = c(0.3, 0.7), scale = "level_pct", pct_type = "row")
get_type(x)
}
\seealso{
\code{\link[=get_scale]{get_scale()}}, \code{\link[=set_scale]{set_scale()}}, \code{\link[=get_pct_type]{get_pct_type()}}, \code{\link[=set_pct_type]{set_pct_type()}}, \code{\link[=fmt_attr]{fmt_attr()}}.
}
