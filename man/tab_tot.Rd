% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-steps-legacy.R
\name{tab_tot}
\alias{tab_tot}
\title{Add totals to a \code{\link[tabxplor]{tab}}}
\usage{
tab_tot(
  tabs,
  tot = c("row", "col"),
  name = "Total",
  totcol = "last",
  data = NULL
)
}
\arguments{
\item{tabs}{A \code{tibble} of class \code{tab}, made with \code{\link{tab_plain}} or
\code{\link{tab}}.}

\item{tot}{\code{c("col", "row")} or \code{"both"} print total rows and total columns ;
\code{"row"} or \code{"col"} print only one type ; \code{"no"} removes all totals.}

\item{name}{The names of the totals, as a character vector of length one or two
(\code{c("Total_row", "Total_column")} to name rows and cols differently).}

\item{totcol}{\code{"last"} prints a total column for the last factor column variable only ;
\code{"each"} prints one for each column variable.}

\item{data}{The original database : only useful for mean columns, whose total-row variances
--- needed by \code{\link{tab_ci}} --- can only be computed from the microdata.}
}
\value{
A \code{tibble} of class \code{tab}. Total rows are then detected with
\code{\link{is_totrow}}, and total columns with \code{\link{is_totcol}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}

Deprecated in 2.0.0, defunct in 2.1.0 -- totals are built directly by \code{\link[=tab]{tab()}} (a total row is
always computed, one total column shown). \code{tab_tot()} still works on an existing tab.
}
\examples{
\donttest{data <- dplyr::starwars

data |>
  tab_plain(sex, hair_color) |>
  tab_tot("col", totcol = "each")
  }
}
\keyword{internal}
