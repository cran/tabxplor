% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_reg.R
\name{reg_formulas}
\alias{reg_formulas}
\title{The model formulas a regression table fitted}
\usage{
reg_formulas(x)
}
\arguments{
\item{x}{A table built by \code{\link[=tab_reg]{tab_reg()}}.}
}
\value{
A tibble with one row per model: \code{model} (its name in the table), \code{outcome}, \code{family} (the
outcome family), \code{link} (the measure that model estimates --- the word \verb{link =} takes), \code{fit}
(the R call it was fitted with) and \code{formula}. A \code{svyglm()} row also means robust
(Huber-White) standard errors: survey's design-based variance IS the sandwich, which is why a
ratio or a difference on a binary outcome is fitted through it.
}
\description{
Shows the formula behind every column of a \code{\link[=tab_reg]{tab_reg()}} table --- exactly what reached
\code{\link[stats:glm]{stats::glm()}}, \code{\link[survey:svyglm]{survey::svyglm()}}, \code{\link[nnet:multinom]{nnet::multinom()}} or \code{\link[MASS:polr]{MASS::polr()}}. Use it to check what a
\verb{shape =}, a \verb{trials =} or a model formula really built.
}
\details{
One row per model: several \code{outcome}s give one each, a \code{predictors} list one per model. Two things
the list does not repeat: under \code{tab_vars} the same formula is fitted \strong{within each group}, and
\code{color = "between_groups"} (or \code{stats = "group_interaction"}) fits one extra pooled model for the footer
test only.

A summed score (\verb{trials =}) is fitted on a success / failure pair, so its formula names the two
internal columns tabxplor builds for it (\code{.gb_succ}, \code{.gb_fail}, and \code{.gb_trials} in the offset of
the risk-ratio link).

The formula names the columns as the user wrote them, but a continuous predictor is fitted
\strong{anchored} at its \code{ref} (its mean by default), and a \verb{shape =} may have recoded it --- neither
changes any effect, only what the Constant row means.
}
\examples{
\donttest{
d <- forcats::gss_cat
d$married <- as.integer(d$marital == "Married")
reg_formulas(tab_reg(d, "married", c("race", "age"), family = "binomial"))
}
}
\seealso{
\code{\link[=tab_reg]{tab_reg()}}, \code{\link[=reg_measures]{reg_measures()}} (what an outcome can be modelled as).
}
