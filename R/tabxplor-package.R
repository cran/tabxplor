# PURPOSE: the package-level help topic, `?tabxplor`.
# ROLE: roxygen builds it from DESCRIPTION (title, description, author, the two URLs); it carries
#   `\keyword{internal}`, so it is a landing page for `?tabxplor`, not a reference-index entry.

#' @keywords internal
"_PACKAGE"
